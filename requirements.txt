# Dependencias para scraping y procesamiento de datos de estudiantes
# Versión optimizada - solo lo estrictamente necesario

# Cliente de Supabase
supabase==2.3.4

# Variables de entorno
python-dotenv==1.0.1

# HTTP requests para scraping
requests==2.31.0

# Parsing HTML
beautifulsoup4==4.12.3

# Opcional pero recomendado: parser más rápido para BeautifulSoup
lxml==5.1.0